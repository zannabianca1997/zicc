type Int = int;

type PtrAbsolute = &int;
type PtrStack    = @int;

type Array = [int; 5];

type BinFun   = fn(int, int) -> int;
type PtrToFun = & fn(int, int) -> int;

type Point = struct Point {
    x: int,
    y: int,
    z: int
};
type IntOrFun = struct IntOrFun {
    tag: int,
    val: union {
        n: int,
        f: & fn() -> int
    }
};